{
  "name": "nenkraft",
  "description": "none",
  "author": "Nuuf",
  "version": "0.1.10",
  "devDependencies": {
    "autoprefixer": "*",
    "css-loader": "*",
    "eslint": "^4.0.0",
    "extract-text-webpack-plugin": "^2.*",
    "file-loader": "*",
    "html-webpack-plugin": "^2.*",
    "postcss-loader": "*",
    "rimraf": "*",
    "style-loader": "*",
    "url-loader": "*",
    "webpack": "^2.*",
    "webpack-dev-server": "^2.*"
  },
  "scripts": {
    "serve": "SET NODE_ENV=development&& node deve-server.js",
    "build": "rimraf dist && SET NODE_ENV=production&& webpack",
    "clean": "rimraf dist",
    "lint": "eslint src",
    "lint-init": "eslint --init"
  }
}