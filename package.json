{
  "name": "nenkraft",
  "description": "none",
  "author": "Nuuf",
  "version": "0.4.3",
  "main": "index.js",
  "devDependencies": {
    "autoprefixer": "*",
    "colors": "*",
    "copyfiles": "*",
    "cross-env": "*",
    "css-loader": "*",
    "eslint": "^4.0.0",
    "extract-text-webpack-plugin": "^2.*",
    "file-loader": "*",
    "html-webpack-plugin": "^2.*",
    "postcss-loader": "*",
    "raw-loader": "^0.5.1",
    "rimraf": "*",
    "style-loader": "*",
    "url-loader": "*",
    "webpack": "^2.*",
    "webpack-dev-server": "^2.*"
  },
  "scripts": {
    "s-d": "cross-env NODE_ENV=development",
    "s-p": "cross-env NODE_ENV=production",
    "clean": "rimraf dist && rimraf server/public/nk.min.js",
    "copy-to-server": "copyfiles -f dist/nk.min.js server/public/",
    "copy-un-files": "copyfiles -f assets/xhr/* dist/assets/xhr",
    "lint": "eslint src",
    "lintf": "eslint src --fix",
    "start": "npm run s-d node deve-server.js",
    "build-dev": "npm run clean && npm run s-d webpack",
    "build": "npm run clean && npm run s-p webpack",
    "build-x": "npm run clean && npm run s-p webpack && npm run s-p webpack -- --env.bigBundle && npm run copy-to-server && npm run copy-un-files",
    "electron": "electron ./electron/main.js"
  }
}